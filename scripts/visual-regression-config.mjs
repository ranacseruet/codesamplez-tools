export {
  VIEWPORT_PRESETS,
  VISUAL_NAVIGATION_TIMEOUT_MS,
  VISUAL_SETTLE_DELAY_MS,
  splitCommaList,
  loadVisualRegressionConfig,
  resolveFromWorkingDirectory,
  selectConfiguredRoutes,
  selectRoutesForChangedFiles
} from '../visual-diff-github-action/lib/visual-regression-config.mjs';
