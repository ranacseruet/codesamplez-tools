# Enforce the package.json "engines" range (Node >=24.15.0 <25) at install
# time. Without this, npm only warns and the mismatch surfaces later as 17
# red Jest suites ("Must use import to load ES Module") instead of a clear
# install-time error.
engine-strict=true
